<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="/util.css" />
  </head>
  <body>
    <header class="header container-lg d-flex align-items-center">
      <div class="container g-0">
        <form action="/search">
          <div class="row d-flex g-0 align-items-center">
            <div class="col-3">
              <a href="/">
                <h1 class="logo">
                  <img class="logo-img" src="/image/logo.PNG" alt="다이닝코드" />
                </h1>
              </a>
            </div>

            <div class="col d-flex justify-content-center searchBar">
              <input class="search_box" name="keyword" id="keyword" placeholder="지역,음식 또는 식당명 입력" />
              <button id="searchBtn"><i class="bi bi-search"></i></button>
            </div>

            <div class="col-3 d-flex justify-content-end">
              <ul class="user d-flex justify-content-end">
                <% if(userId) { %> 
                <%if(name == "admin"){%>
                <li>관리자</li>
                <li><a href="/add">관리자페이지</a></li>
                <li><a href="/myPage/<%= userId %>">마이페이지</a></li>
                <li><a href="/logout">로그아웃</a></li>
                <% } else { %>
                <li><%= name %>님</li>
                <li><a href="/logout">로그아웃</a></li>
                <li><a href="/myPage/<%= userId %>">마이페이지</a></li>
                <li><a href="/review/<%= userId %>">리뷰</a></li>

                <li><a href="/myReview/<%= userId %>">작성한 리뷰</a></li>
              <%}%>
            <%}else{%>
              <li><a href="/login">로그인</li> 
              <li><a href="/join">회원가입</a></li> 
            <%}%>
            
          </ul>
        </div>

               
        </form>

      </div>
    </header>

    <script>
      const input = document.getElementById('keyword');
      const btn = document.getElementById('searchBtn');

      btn.addEventListener('click', function (e) {
        console.log(input.value);
        if (input.value === '') {
          alert('검색어를 입력해주세요');
          e.preventDefault();
          return;
        }
      });
    </script>
  </body>
</html>
