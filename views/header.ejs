<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/util.css">
</head>
<body>
  <header class="header container-lg d-flex align-items-center">
    <div class="container g-0">
      <form action="/search" >
      <div class="row d-flex g-0 align-items-center">

        <div class="col-3"><a href="/"><h1 class="logo">LOGO</h1></a></div>
        <div class="col d-flex justify-content-center"><input class="search_box" type="text" name="keyword" placeholder="지역,음식 또는 식당명 입력"><button>검색</button>
        </div>
        <div class="col-3 d-flex justify-content-end">
          <ul class="user d-flex justify-content-end">
            
            <%if(userId){%>
              <%if(userId == "root"){%>
                <li>관리자</li>
                <li><a href="/add">관리자페이지</a></li>
                <li><a href="/myPage/<%= userId %>">마이페이지</a></li>
                <li><a href="/logout">로그아웃</a></li>
              <%}else{%>
                <li><%= userId %></li> 
                <li><a href="/logout">로그아웃</a></li>
                <li><a href="/myPage/<%= userId %>">마이페이지</a></l>
              <%}%>
            <%}else{%>
              <li><a href="/login">로그인</li> 
              <li><a href="/join">회원가입</a></li> 
            <%}%>
            
          </ul>
        </div>
      </div>
      </form>    
    </div>
  </header>
</body>
</html>